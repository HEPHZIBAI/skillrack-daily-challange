/*
A table called student is created with the following DDL command:

CREATE TABLE student (id INT, name VARCHAR(30), arrearscount INT);

Write the SQL statement to delete the records of the students who are not having arrears.
*/

delete from student where arrearscount b;